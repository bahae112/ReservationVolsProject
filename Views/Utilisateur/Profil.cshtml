@{
    ViewData["Title"] = "Mon Profil";
}

<h2 class="text-center my-4">👤 Mon Profil</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success text-center">@TempData["Message"]</div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger text-center">@TempData["Error"]</div>
}

<div class="container">
    <form asp-action="ModifierProfil" method="post">
        <div class="mb-3">
            <label class="form-label">Nouvel Email 📧</label>
            <input name="Email" class="form-control" placeholder="Entrez votre nouvel email" value="@User.Identity.Name" />
        </div>

        <hr />

        <h4>Modifier le mot de passe 🔒</h4>
        <div class="mb-3">
            <label class="form-label">Ancien Mot de Passe</label>
            <input name="AncienMotDePasse" type="password" class="form-control" placeholder="Entrez votre ancien mot de passe" />
        </div>

        <div class="mb-3">
            <label class="form-label">Nouveau Mot de Passe</label>
            <input name="NouveauMotDePasse" type="password" class="form-control" placeholder="Entrez votre nouveau mot de passe" />
        </div>

        <button type="submit" class="btn btn-primary">Modifier</button>
    </form>
</div>
